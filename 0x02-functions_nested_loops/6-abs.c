#include "main.h"

/**
 * _abs -Computed the obsolute value on an integer.
 * @n:	The interger to be computed.
 *
 * Return: The absolute value of the integer.
 */
int _abs(int n)
{
if (n >= 0)
return (n);
else
return (-n);
}
